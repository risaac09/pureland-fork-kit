# Changelog

Entries name the change and the pull request. The reasoning behind a change lives in the pull request and in [AI-ASSISTANCE.md](AI-ASSISTANCE.md). The sources behind a claim live in [PROVENANCE.md](PROVENANCE.md). Each of the three records warrants one thing.

## Unreleased

- Filled the four `Human review: Pending` rows in `AI-ASSISTANCE.md` for #54, #57, and #58 with the maintainer's statement that he read each diff and merged on green CI, dated to the merges. (#59)
- Gave the improvement protocol a row for an instrument arriving rather than changing, and paid it on the instrument that found it. Every row in the triage table described changing something that already existed, so #44's new script fit none of them and landed carrying a falsifiable claim with no row in `RESEARCH-STATUS.md`, no entry in `TOOLBOX.md`, and one mention in the changelog: findable by nobody reading the toolbox and unknown to the claim ledger. A new instrument now owes a claim row with its falsifier, a toolbox entry, and a statement of what a first use would have to show for it to have failed its own test. `scripts/ecosystem_inventory.py` is now in `TOOLBOX.md` and carries a row stating the falsifier the script already states about itself: if the estimates match the counts, the counting added nothing the person could not already say. No run exists. #44 predates the row, so this is a gap the protocol now catches rather than a rule anybody broke. Two batches have now been triaged and both found something wrong with the table, which is recorded rather than corrected away. (#58)
- Added `scripts/ecosystem_inventory.py`, a read-only local count of one person's own information surfaces: it reads only surfaces marked `yes` in a permission record written before its first read, takes names and modification dates without opening a file, writes one Markdown file, overwrites nothing, arms an audit hook that raises on any network event, and prints what it read, wrote, refused, and the command that removes what it made. No instrument document ships with it; a first run decides whether one should. (#44)

## 0.1.1 (2026-09-19)

Twenty pull requests are named below. The method's six steps took their current names, the private path and the model-use packet opened two more ways in, the front page and the visual system were rebuilt, the capture guide and the filming forms joined the toolbox, and the checker grew the record gates, the classification gate, and the `performed` gate.

The evidence did not move. This release ships no field trial, no second reader, and no independent application, so it is a patch on 0.1 rather than the [version 0.2 gate](TESTING.md#provisional-version-02-planning-target), which asks for five to ten independent applications across at least three contexts and has none. [CURRENT-EVIDENCE.md](CURRENT-EVIDENCE.md) caps what the kit has shown, and this release does not raise it.

- Cut release 0.1.1 and repaired the test that cutting it broke. `test_unreleased_entries_warn_from_changelog` read the repository's own `## Unreleased` section and passed because entries happened to be there, so emptying that section at release turned a true statement about the drift rule into a failing test. It now seeds its own entry, as the sibling test covering the empty case already did, and it was verified against a deliberately disabled drift rule to confirm it can still fail. The rule itself is unchanged. (#57)
- Carried PR #45's two fuller disclosure rows into the 2026-09-06 entry in `AI-ASSISTANCE.md`. The entry was already on `main`, folded in by #48 and credited to #45 by link, with corrections and human review recorded as `Owed` and nothing more. #45 said why: no human had read the branch, the maintainer's gate outside this repository had not been met, and a blind cross-read by other models is not human review, which is a claim about what does not count as review and belongs under a rule that asks how a human verified the output. Both rows are written to the round rather than the present, because the pull requests #45 described as held have since merged. #45 is closed as absorbed, with its public reason on the pull request. (#56)
- Added `IMPROVEMENT-PROTOCOL.md`: how a finding about the kit becomes a change to it. It triages a finding by what it lands on, from prose through an instrument, a construct, the schema, a falsifier, the ledger, and the thesis, and states what each class owes: a re-read of every record scored under a changed instrument, a fixture that fails before and passes after a changed check, and both readings kept when a construct moves. It forbids the revision-level counterparts of the moves a run may not make, including weakening a falsifier close to failing, loosening a check to turn a failure into a pass, editing a record to make it conform, and raising the evidence ceiling by any route but an accepted trial. Declined proposals get a register. The seven proposals from the agent-lane walk are triaged as the worked batch; this change makes none of them, and #55 made all seven before this one landed, so the batch reads as a worked example rather than an open queue. Linked from `METHOD.md`, `TOOLBOX.md`, `CONTRIBUTING.md`, and `llms.txt`; its two falsifiers have rows in `RESEARCH-STATUS.md`. The procedure is untested and does not constrain the maintainer who wrote it. (#54)
- Added `research/agent-lane-walk-2026-09-19.md`: five readings of the repository by a model, from a different position each time, with four mutation probes against the schema and checker. It reports that a record can set `human_observe.status` to `performed` with a null performer, an empty evidence array, and design analysis declared substituted, and still pass the structural gate; that `templates/field-test.md` carries no ceiling sentence and `EXTRACTION-CHECK.md` links to no evidence record; and that `llms.txt` understates the guard on a combined score, which the schema's closed objects hold rather than the three-name scan. Seven corrections are proposed and none is made here; #55 made all seven. The walk is a dated reading of the repository at `7543635` and is not revised to match what the repository became. The walk is not a field test, enters no ledger, and raises no ceiling. (#54)
- Put the smallest beginning first. The front page and the README each offered two ways in and both were reporting instruments, while the private path in `METHOD.md`, one pass through the same six steps that keeps no record, was reachable only by reading the method far enough down to find it. `index.html` and the README now open the start with it, above the self-guided and model-use packets, and the README points a model at `llms.txt` as its lane. The front page strip takes the full width rather than a third column, because a path that records nothing is not a peer of the two that do. Scope asked for a disconfirming condition without showing one, so it now links FT-001's, written before analysis, next to what the assessor concluded against it, and reports the assessment that came back against it: `mixed`, with no comparator run, so the record could not settle whether the additional readings came from the method. An example of a real condition returning unsettled is worth more than another instruction to write one. (#55)
- Made the evidence ceiling travel with the artifact. `templates/field-test.md` and `EXTRACTION-CHECK.md` now each state what completing them establishes, what it does not, and where the evidence record and the claim-by-claim status live. Neither carried any of that before, so a copied report or a deep link into the check arrived with its limits stripped off, and the extraction check refused certification in its last line while never naming itself unvalidated. Corrected the composite-score line in `llms.txt`, which credited the wrong guard: every object in the JSON record is closed, so the schema rejects any key it does not name, including names `check_repo.py` does not list, and the checker's three literal names are a second pass over that same closed surface. Prose is the open surface. Nothing checks a sentence that averages the bands or reports one overall figure, and that is where the refusal is the reader's to keep. (#55)
- Closed the `performed` gap in `data/field-test.schema.json`: a record cannot read `human_observe.status: performed` while the performer is absent, the evidence list is empty, or AI or design analysis was substituted in place of a person. The forged record passed both the schema and `check_record_rules()` with zero errors before this change. The label is load-bearing, because two guards in `scripts/check_repo.py` read `status != "performed"` to refuse a completion claim and a support claim, so a forged `performed` switched both guards off rather than sitting inert. A code-review pass found the gap surviving by another name, since `minLength` counts characters rather than content and a performer of `"   "` was accepted again, so the rule also requires a non-whitespace match on the performer and on every evidence item. `templates/field-test.md` and `TESTING.md` now state the binding, so a person who writes `performed` learns what it commits instead of meeting a conformance error that names JSON paths. `scripts/test_performed_gap.py` holds the probe that passed the old schema, each single-field forgery on its own so the gap cannot survive by another name, both whitespace variants, and an assertion that the rule refuses nothing it was not given; it also holds a legitimate `performed` record and FT-001's published record, which must still pass. The suite exits 1 against the pre-repair schema, so it can fail. CI runs it on every pull request and every push to `main`. FT-001 is untouched: it reads `not-performed`, and the rule constrains the `performed` branch only, so a record stays free to carry evidence explaining why Attend did not happen. (#55)
- Added a collaborator theory-of-change entry to `PROVENANCE.md`: Samara's Sanctuary Studios' published sentence, "Self-love in collective resonance harmonically organizes prosperous work," cited by name and primary link, with what it supports (an outcome framing the maintainer is drafting toward), what it does not (no version 0.1 claim, no evidence about this method), and the maintainer's working relationship with its founder. (#52)
- Promoted Field & Form to the front page, the README, and the active design assets. `index.html` is the reconciled draft with its asset paths resolved and anchors for the retired `#spine` and `#readerships` fragments; `design/tokens.css`, `design/styles.css`, and `design/theme.js` are the active system; `design/tokens.json` mirrors the token file and `scripts/check_repo.py` fails when a value differs, with regression tests; the README takes the candidate with a paper and ink banner pair; `design/VISUAL-LANGUAGE.md` describes the system and the standalone mark files take its palette. Ink & Bone, its tokens, the EB Garamond and italic Cormorant subsets, and the `design/v2/` draft with its specification, review, and specimen board live complete at `0e853f5`. No first-time reader has read the page. (#51)
- Added `CAPTURE-GUIDE.md`: what to record during a run, what each kind of raw material can stand behind, and the rule that raw material stays in private custody while the record cites it by ID. Linked from `TOOLBOX.md` and `llms.txt`; its two falsifiers have rows in `RESEARCH-STATUS.md`. (#48)

- Added the Field & Form second visual-system draft under `design/v2/`: a homepage, a specimen board, a README candidate, a specification with an integration sequence, and the review that measured the live page against it. The active Ink & Bone system, the root page, and the README are unchanged. (#50)

- Restore sourced AI disclosures and missing PR references, update live Attend vocabulary, and identify the commit requested by Stage 1 intake. (#48)

- Complete model-index discovery, serve checker and citation links as raw text, and test repository-owned raw URL validation. (#48)

- Put the method's route paragraph first and point duplicated pilot descriptions to the offering. (#48)

- Shorten the front-page entry, route Field Pilot proposals through the offering, and link the reply to its canonical page. (#48)

- Move the practice frame's self-observation prompts before its theory and link the extraction check to those prompts. (#48)

- Put each instrument's use before its place in the method and link the scorecard's counting instruction to Scope. (#48)

- Disclose the reader-entry and model-discovery work on both maintenance branches. (#48)

- Tightened record checks after malformed reports passed review probes: compare the identity fields and ledger version column, match the exact artifact version, and require the kit commit to remain reachable from a Git ref or HEAD. Added seven regression cases, including compatibility with the current report template. Added the charter requirement to Stage 1 intake and the action baseline, materiality rule, and challenge route to the self-guided packet. (#47)

- Added three public routes into the method: the self-guided human packet, the model-assisted packet, and the Field Pilot intake. The Field Pilot can use commissioned, external-funding, or limited-voluntary terms, while price and the rest of the agreement stay in a private conversation after Stage 1. (#47)
- Added the Field Pilot charter and cross-artifact record gates. The checker now ties every conformant record to one ID, filename, report, ledger row, artifact version, and reachable kit commit; prints independent-record and distinct-practice counts; and warns when the changelog contains unreleased changes. The warning requires a separate check of live Pages and release state. (#47)
- Added four per-test predeclaration lines to `templates/field-test.md`: which of the three uncertainties the test speaks to, the comparator, the success condition, and the likely competing explanation. The classification rule and the before-a-test list in `TESTING.md` are unchanged. (#43)
- Named the understandable count in `SCORECARD.md` a documentation indicator. (#42)
- Added a predefined agency action and a materiality-rule question to Scope in `templates/run-with-a-model.md`, reworded the Stage 1 sentence so the completed record no longer reads as entering through scoping, and warned against saving a draft under `data/`. (#41)
- Added the decision-authority questions to Scope in `METHOD.md`: what the person can change alone, who else decides, what follows if they refuse, and the dependence worth keeping. (#40)
- Added a private path to `METHOD.md`: one pass through the six steps that keeps no record, with the rights boundary unchanged and no research claim. (#39)
- Removed the claim in `METHOD.md` that the field pilot is paid and the "or not at all" exclusivity; `OFFERING.md` is the one place that states the pilot's terms. (#38)
- Closed the support-classification gap in `scripts/check_repo.py`: a record cannot claim `supports-tested-context` without an improved predefined action, performed human Attend where required, every party's follow-up impact resolved, a closed follow-up window, and no required step left incomplete. `scripts/test_classification_gap.py` holds the fixture that passed the old rule with zero errors, in a pending-status, an unmeasurable-status, and an incomplete-step variant, and CI runs it on every pull request and every push to `main`. (#37)
- Pointed the README's opening at the public front page. The site URL now sits directly under the title, naming the repository as the record behind it, and the sentence that carried the URL from the middle of "Use the method" drops it rather than leaving a second copy to drift. (#36)
- Renamed the method's vocabulary. The six steps are Scope, Attend, Read, Trace, Adapt, Report, and a person runs the method rather than walking a journey through stations. `JOURNEY.md` folded into `METHOD.md`, which now carries the six steps, the branch questions, the Alchemy and model companions, and the reply at Report; the old file lives complete at commit `bc833b7`. `templates/walk-with-a-model.md` became `templates/run-with-a-model.md`. The free reading at Report became a reply: write to the maintainer's address with what you did, get one reply, and nothing is done with it without asking; consent for any specific use is asked on the actual artifact, when a use exists. The template's coverage field and the issue form's dropdown are labelled coverage, so the step named Scope and the record's `scope` object do not share a label. Records keep the vocabulary they were written in: FT-001's report and JSON, the changelog, and the AI-assistance record are unchanged, and the schema keys `walking_person`, `station_completion`, and the `journey` instrument value stay as the version 0.1 data contract, with the mapping stated in `METHOD.md`. (#35)
- Put the ask at the Return station. `JOURNEY.md` station 6 offers one free reading of a cleared report behind the existing public-safe gate, names the two scales a reading can answer at, and places the field pilot inside that exchange rather than in the free companion tool. `templates/field-test.md` gains a return route that records nothing in the JSON, `OFFERING.md` names the reading as a second way in, and FT-001 carries the route as an addendum and one follow-up evidence line, with its window and executed adaptation unchanged. (#34)
- Added `templates/walk-with-a-model.md`, the packet a person pastes into a language model to walk the journey on their own use of language models, wired into `llms.txt`, `TOOLBOX.md`, `JOURNEY.md`, `README.md`, `CROSSWALK.md`, `RESEARCH-STATUS.md`, `index.html`, and `REQUIRED_ARCHITECTURE`. The agent lane said what a model may not generate and never said what a person and a model do together. (#33)

- Point scoping and reply guidance to canonical terms, align contribution licenses with file ownership, and remove license-file metadata. (#49)

## 0.1.0 (2026-09-01)

The first release. Everything below was written between 2026-08-17 and 2026-09-01 and compressed to this register on release. The long-form entries live in the history at commit `3bec315` and before.

- Gave the walk one door and one ceiling: the header nav collapsed to journey, evidence, and the record, the closing section points at the field pilot and the crosswalk, the eyebrow dropped its unguarded version numeral, the three faces are self-hosted as latin woff2 subsets under the OFL, and `index.html` joined `REQUIRED_ARCHITECTURE`. `scripts/check_repo.py` ties the page's one ceiling sentence to `CURRENT-EVIDENCE.md`'s first sentence, requires the page to carry the release-version sentence, and resolves every stylesheet `url()`. (#31)
- Thinned the front page to the walk, removing the toolbox card section. The load-bearing limits and governance-risk sentences moved into the research-spine band. (#30)
- Choreographed the walk with scroll-linked motion built from CSS view timelines, so the drawn thread tracks the reader's scroll position instead of rewarding progress. Motion falls back to draw-once without browser support. (#29)
- Rebuilt `index.html` as a drawn walk, giving each station its own line-work diagram on a single connecting thread. The evidence section pairs FT-001's record with labelled empty frames. (#27)
- Renamed the five hindrances to the maintainer's practice vocabulary, with the Pali terms kept alongside them in `PRACTICE-FRAME.md`. `PROVENANCE.md` keeps the cited translation's own wording. (#28)
- Tied the prose copies of the follow-up review date to the record: `check_follow_up_date_copies()` checks the `CURRENT-EVIDENCE.md` ledger row against `follow_up.review_date`, so a changed window can't leave a stale date standing. (#26)
- Named technology theory as a field the kit already used, adding it to the fields-and-limits table in `THESIS.md` with the boundary the other fields carry. (#25)
- Folded `AGENT-READING.md` into `llms.txt` and moved `READERSHIP.md`'s thesis statement into `THESIS.md`. `READERSHIP.md` lives complete at commit `bc20be3`, and `CROSSWALK.md` survives as the one remaining map. (#24)
- Merged `HYPOTHESIS.md` into `TESTING.md`, narrowing the public research functions and removing duplicated required-observations text. The secondary hypotheses now point at the `RESEARCH-STATUS.md` ledger. (#24)
- Named Rubinstein Productions as the offering behind the field pilot in `OFFERING.md`'s opening line, and pointed its what-happens-together list at the journey instead of restating it. (#24)
- Kept `PROVENANCE.md`, `AI-ASSISTANCE.md`, and `CHANGELOG.md` as separate records, each warranting a different thing, and named the oldest AI-assistance entry's disclosure gap. (#24)
- Cut root documentation and named the doctrine governing what stays: repeated evidence sentences merge into `CURRENT-EVIDENCE.md`'s ledger, `FIELD-TESTING.md` merges into `TESTING.md`, and `BRIEF.md` is retired. (#23)
- Removed the open-model lane, `OPEN-MODEL-LANE.md`, `scripts/llm-lane.sh`, and `templates/bench-card.yaml`, off-register for a research-program prototype. The files live complete at commit `0e55746`. (#23)
- Moved the data-maturity audit to `research/data-maturity-audit.md` in the open research lane, and added `CURRENT-EVIDENCE.md` to `TOOLBOX.md`'s index. (#23)
- Put the repository's own rendered GitHub links under the link check and added a `--list-architecture` flag to `scripts/check_repo.py` that prints `REQUIRED_ARCHITECTURE` on request. It refuses to run beside another flag. (#21)
- Named the repository's two readerships and added `AGENT-READING.md` (the agent lane), `CROSSWALK.md` (the element crosswalk), and `READERSHIP.md` (the vertical view). (#21)
- Retired the adapted ensō-fork mark for a fork diagram built from the papañca causal sequence, no circle and no return reading. Updated the mark assets, `design/VISUAL-LANGUAGE.md`, and `PROVENANCE.md`'s naming section to match. (#19)
- Added RAIN (Recognize, Allow, Investigate, Non-Identification) to `PRACTICE-FRAME.md` as the practice loop's method for a noticed condition, citing Tara Brach's wording in `PROVENANCE.md`. (#19)
- Added the Madhupiṇḍika Sutta's proliferation chain and the Abhidhamma's javāna account to `PRACTICE-FRAME.md` as the mechanism underneath the poisons and hindrances, cited in `PROVENANCE.md`. Added `ORIGIN.md` as a placeholder for a Zen-practice history. (#19)
- Tightened `llms.txt` after review, separating the evidence ceiling's link from its description and restating the durable limit instead of a count that goes stale. Added a check tying the release version to `CITATION.cff`. (#18)
- Added `llms.txt` as the model-facing entry point, with absolute Pages links, the evidence ceiling, and rights and repository boundaries. Extended `scripts/check_repo.py` to require and check the file. (#16)
- Added the open-model lane, `OPEN-MODEL-LANE.md`, `scripts/llm-lane.sh`, and `templates/bench-card.yaml`, generalized from the maintainer's private stack with hostnames and credentials replaced by examples. (#15)
- Reframed PureLand as a version 0.1 research-program prototype with public functions for thesis, method, toolbox, hypothesis, testing, and current evidence. (#13)
- Integrated the architecture, measurement, and rights lanes, removed unpublished compatibility layers, made public issues scoping-only, and replaced the Boolean privacy check with an artifact-version public-safe review. (#13)
- Narrowed FT-001 and Alchemy claims, removed an unsupported information-theory contribution, and added boundaries around the PureLand name and generosity definition. (#13)
- Wired `index.html` up as the repository's front page, linking out to the kit's documents and consuming `design/tokens.css` and the marks in `design/assets/` instead of forking them. (#10)
- Dropped the Google Fonts request from `index.html` in favor of the documented fallback stacks, matching the no-servers posture of the rest of the kit. (#10)
- Replaced `index.html`'s superseded three-layer ledger with the stations from `JOURNEY.md`, and corrected `design/VISUAL-LANGUAGE.md`'s stale description of the roman numerals. (#10)
- Set the `index.html` hero question in sentence case; letterspaced capitals ran long and shouted, against `design/VISUAL-LANGUAGE.md`'s first principle. (#10)
- Fixed the `index.html` spread layout below the phone breakpoint, where marginalia stayed pinned to its grid column and collapsed the passage column. (#10)
- Fixed a dark-mode contrast defect in `design/tokens.css` and added the `--measure` and `--color-rule` tokens `index.html` needed, aliasing `--rule-hairline-color` to `--color-rule`. (#10)
- Added `.nojekyll` so Pages serves the repository as static files with no build step. (#10)
- Widened `scripts/check_repo.py` to read HTML links, validate field-test records against a schema, and warn on unreachable content files. (#11)
- Moved schema validation onto `jsonschema` and added `requirements.txt` for CI, replacing a hand-rolled validator that failed open on the privacy gate. (#13)
- Scoped `JOURNEY.md`'s Alchemy claims to what FT-001 supports and matched README's description of the app to it. (#9)
- Made `PROVENANCE.md` and `AI-ASSISTANCE.md` reachable from the README via `research/README.md`, and linked the provenance record from `BRIEF.md` and `data/README.md` from `CONTRIBUTING.md`. (#8)
- Reconciled README's description of `FIELD-TESTING.md` with `JOURNEY.md`: it is the instrument the ground and return stations use directly, not a discipline running alongside them. (#8)
- Fixed `CITATION.cff`, which named CC BY-SA 4.0 as the sole license; it now lists both MIT and CC BY-SA 4.0. (#8)
- Marked FT-001's bounded adaptation executed: the Alchemy README now documents the inbound route for corrections and field reports. The record moves from proposed to executed. (#7)
- Rewrote the public documents in plainer language, keeping every guardrail, claim boundary, and file name intact. (#4)
- Replaced "the four accesses" and their formal names with four plain questions: understandable, reachable, adaptable, and traceable. (#4)
- Recorded FT-001, a maintainer-side partial dry run of the journey on Alchemy, not independent. The ledger entry lives in `FIELD-TRIALS.md`. (#13)
- Consolidated the entry path into one curated journey, `JOURNEY.md`, with the instruments intact as stations and the README opening with the journey. (#5)
- Connected the Alchemy app into the journey as the companion for walking it on your own information ecosystem. (#5)
- Added `journey` to the field-test instrument options in the schema, issue form, and template, so a report can cover the whole walk. (#5)
- Reorganized the public surface around goals, method, accountability, the field-pilot offering, and evidence still to be tested. (`01a3f21`)
- Added `OFFERING.md` for the bounded PureLand Field Pilot and `research/README.md` for the public open-research lane. (`01a3f21`)
- Kept the workspace research reservoir, participant material, and other protected records outside the public release surface. (`01a3f21`)
- Added the Ink & Bone visual language, design tokens, the ensō-fork mark, and a README banner. (#2)
- Reframed the kit around attention sovereignty, the three poisons, five hindrances, skillful means, greed, and generosity. (#1)
- Added consent, AI-system, research-status, field-testing, rights, contribution, and governance guidance. (#1)
- Retired the unvalidated extraction coefficient and composite thresholds. (#1)
- Added canonical CC BY-SA 4.0 legal text and clarified license boundaries. (#1)
- Added validation automation and structured field-test templates. (#1)
- Initial protocol, scorecard, extraction check, provenance, and license explanation. (`b58014b`)
