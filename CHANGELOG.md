# Changelog

## Unreleased

- Named technology theory as a field. The kit already used the lens without naming it: the AI system annex, the Open Source AI Definition and NIST AI Risk Management Framework references in `PROVENANCE.md`, the datasheets and model-cards lineage, and the agent lane. The fields-and-limits table in `THESIS.md` gains the row, with its boundary stated the same way the other three carry theirs: reading a system's documentation does not establish what the system does. The each-field-fails paragraph gains the matching clause, and `index.html` moves with it, now saying body-based practice where it said somatic practice, so the two surfaces carry one wording. The maintainer directed the addition.

- Cut the reading apparatus from three documents to one. `AGENT-READING.md` at 142 lines, `CROSSWALK.md` at 254, and `READERSHIP.md` at 122 came to 518 lines about how the repository is read, more than the whole instruments cluster. A fork wants the tools and one map. `AGENT-READING.md` folds into `llms.txt`, which is already the model-facing entry point, already the published convention a fetcher looks for by path, and already swept by `scripts/check_repo.py` for links, anchors, placeholder tokens, and the release-version tie, so the folded rules keep every check they had as a Markdown file. The fold also puts the prohibitions where a model lands. Until now `llms.txt` said to read the agent lane next, and a model that stopped there had the ceiling and none of the never-generate list. `READERSHIP.md` leaves the kit. Its one durable sentence, that PureLand is a set of instruments whose validity depends on who used them, is a thesis statement and moves to `THESIS.md` with the reason a reader that cannot be changed by the material cannot test the thesis; its two residue lists were the crosswalk's rows in another order, except the deterministic re-checking note, which moves to the checker's entry in `llms.txt`; its argument for why the residue stays unreconciled moves to the crosswalk's head. The file lives complete at commit `bc20be3`, and the intended destination is `stack-data/context/`, where the internal theory lives. Placement happens outside this repository. `CROSSWALK.md` survives as the one map, cut from seventeen elements to twelve and from 254 lines to 179. Reading order and the front page described design decisions the two entry points now state themselves, and the front-page row recorded a bug this changelog already holds. The name collision between `AI-SYSTEM-ANNEX.md` and `AGENT-READING.md` dissolves with the second file, so `llms.txt` carries the one line that keeps a reader from opening the annex looking for the lane. Entry point and the architecture contract went for the same reason: `llms.txt` now states the prohibitions before the invitation, and it states the contract, how to print it, and why no prose copy exists. Every empty cell stays. `REQUIRED_ARCHITECTURE` loses both files in the same commit, and `README.md`, `TOOLBOX.md`, `JOURNEY.md`, `THESIS.md`, and `index.html` move with them.

- Put the claim at the head of the program that tests it. `HYPOTHESIS.md` merges into `TESTING.md`, and the public research functions go from six to five: thesis, method, toolbox, testing, current evidence. Four containers held the evidence apparatus for a program with one maintainer-side dry run, and two of them were one argument split in half. `TESTING.md` already opened by saying every test begins with a result that would count against the tested hypothesis, then pointed at another file for the hypothesis. Two duplications died in the merge. `HYPOTHESIS.md` carried its required observations twice, once as a column of its own constructs table and once as a table beneath it, and the during-a-test list in `TESTING.md` stated the same rules a third time as procedure; the standalone table is gone, and the two clauses it alone carried, that an artifact denominator is not a person count and that conflict is not smoothed into agreement, join the during-a-test list. Four of the five secondary hypotheses were rows of the `RESEARCH-STATUS.md` ledger, which additionally says what evidence each one needs, so the section now points at the ledger and states the fifth claim, that one bounded adaptation can increase a person's control over attention, which no ledger row carried. Refutability is unchanged: the five falsifiers in `PRACTICE-FRAME.md`, `PROTOCOL.md`, and `FILMING-FORMS.md` are untouched, the disconfirming-results list moves whole, the version 0.2 gate keeps its one live statement in `TESTING.md`, and the follow-up window keeps its one live statement in the ledger row. `README.md`, `llms.txt`, `THESIS.md`, `METHOD.md`, `TOOLBOX.md`, `RESEARCH-STATUS.md`, `index.html`, `REQUIRED_ARCHITECTURE`, and `design/VISUAL-LANGUAGE.md` move in the same commit; the visual language had fixed the roman numerals at I through VI for a sequence that just changed length, so it now reads the count from the sequence rather than restating it.

- Said whose offering the field pilot is. `OFFERING.md` never named Rubinstein Productions and read as a freestanding services page. It is the bridge back to the studio, so it says so in its first line: PureLand is the toolbox that practice shares, and the pilot is the same practice walked with Isaac. The file drops from 80 lines to 64. Its seven-step what-happens-together list was the six journey stations plus the private rights review, so it points at the journey and keeps the private rights review that sits outside the stations; the closing decision about what goes public is the Return station's own work and the file now says so. The nine possible outputs read as prose. Nothing left the rights boundary, the refusal-is-valid rule, or the list of what the pilot does not claim.

- Kept the record cluster at three documents and named a gap in it instead. `PROVENANCE.md`, `AI-ASSISTANCE.md`, and `CHANGELOG.md` warrant three different things: a source, a process, and a change. Merging any pair would make one register's warrant read as another's, and a changelog entry is frozen where a provenance section is current, so the two cannot drift into disagreement the way two live copies do. The oldest AI-assistance entry, 2026-08-21, discloses four of the six things the disclosure rule asks for, because the corrections row was added to the format later. It now says so rather than leaving the absence silent.

- Cut the documentation from 33 root documents to 28 and named the doctrine that governs what stays. Four surfaces carried the same sentence about the evidence: `FIELD-TRIALS.md`, the observed-record table in `CURRENT-EVIDENCE.md`, the current-evidence bullets in `RESEARCH-STATUS.md`, and the stage table in `TESTING.md`. Repetition on that scale is a drift surface, not honesty, because the first update that misses a copy leaves a stale claim standing, and the audit already ranked cross-artifact consistency as the weakest dimension the kit has. Three of the four collapse. `FIELD-TRIALS.md` merges into `CURRENT-EVIDENCE.md` as a section called the ledger, with a column for what nobody observed, so the register and the record it caps sit in one file. `RESEARCH-STATUS.md` keeps the ceiling and points at that ledger instead of restating the window date. The stage table in `TESTING.md` stays, because it cuts the record by stage rather than by trial and a testing program that cannot say which stage it has reached is not a program; it now says out loud that the ledger is its source and that an accepted trial changes the ledger row first. `FIELD-TESTING.md` merges into `TESTING.md`, which the two documents already asked for by each telling the reader to follow the other: the program, the gate, and the before, during, and after-use discipline now read as one document. `BRIEF.md` is gone. It called itself the shorter external overview while running longer than `README.md`, and `index.html` already serves the reader it claimed; its glossary of consent, attribution, and value moves to `THESIS.md`, along with its argument that each of the three fields fails on its own, which until now survived only as prose on `index.html`; its four access definitions were already in `SCORECARD.md`, and its field map, swimlane, and nine-step method were already in `THESIS.md`, `OFFERING.md`, and `METHOD.md`. Its opening sentence survives, in `README.md`, as the statement of why the unfinished parts stay visible: `ORIGIN.md` empty on purpose, the crosswalk's empty cells, absence recorded as absence. `REQUIRED_ARCHITECTURE` and `LINK_ROOTS` in `scripts/check_repo.py` move in the same commit as the files, and `llms.txt` moves with every disposition. The two limits ledgers stay in its primary section.

- Spun the open-model lane out of the kit. `OPEN-MODEL-LANE.md`, `scripts/llm-lane.sh`, and `templates/bench-card.yaml` are removed. The lane is on-thesis in motive, since it applies the extraction check's question to where a prompt travels, and off-register in execution: it is the only document in a research-program prototype whose subject is `launchd`, PID files, and API keys, it sits on no station of the journey, `llms.txt` never indexed it, and its own last line records that no field test of it exists. `AI-SYSTEM-ANNEX.md` keeps the substance a reader of the annex needs, that running the assessed model on hardware you own closes two rows of the audited-unit table and settles nothing else. The three files live complete at commit `0e55746`, which is where to lift them from if they get their own repository. No `archive/` directory was added: retired material inside this repository would rejoin the reachability graph and read as current, and a commit named in the changelog makes the history discoverable without making it live.

- Moved the data-maturity audit to `research/data-maturity-audit.md`. It is a dated, maintainer-side, AI-assisted snapshot with no second reader, which is what the open research lane holds, and it was never in `REQUIRED_ARCHITECTURE`, `llms.txt`, or `TOOLBOX.md`. It reads the infrastructure that carries the evidence, so a later audit supersedes it rather than editing it. `research/README.md` now indexes the records in its own lane. Added `CURRENT-EVIDENCE.md` to `TOOLBOX.md`, which had indexed every other spine document and not the one that caps the claims. Fixed `index.html`, whose method ledger printed Trace, Adapt, and Return twice and showed nine rows for six stations.

- Put this repository's own rendered GitHub links under the link check, and gave `scripts/check_repo.py` a `--list-architecture` flag. `check_targets` already rewrote `risaac09.github.io/pureland-fork-kit/` URLs to local paths so `llms.txt` could use the absolute form without losing rot detection. It now does the same for `github.com/risaac09/pureland-fork-kit/blob/main/`, which `index.html` uses so its links render as pages instead of serving raw Markdown a browser downloads. Links to five kit documents were skipped as external and could have rotted silently; they are checked now, and `index.html` uses one link convention throughout rather than two. Only `blob/main` is rewritten, because a link pinned to another ref names a state this working tree cannot speak for. The flag prints `REQUIRED_ARCHITECTURE` on request, so a person can read the architecture contract without opening the script and without a prose copy that would drift from it. It refuses to run beside another flag, because that branch returns 0 without validating anything and a run combining it with `--fail-on-overdue-follow-up` would report success having checked nothing, which the scheduled watch would read as no follow-up being overdue. The crosswalk's architecture row changes from no human counterpart to a derived one, which is a different finding and is recorded as such.

- Named the repository's two readerships and stopped leaving the difference to chance. Added `AGENT-READING.md`, the agent lane: the reading order a model should take, the architecture contract in `scripts/check_repo.py`, the schema and what conformance does not mean, what a model may generate, what it may never generate, and what it must hand back to a person. Added `CROSSWALK.md`, seventeen elements of one lane set against what mimics them in the other, with the empty cells kept as findings rather than filled by invention; the Observe station is the load-bearing one, and `FT-001` is the record of what happens without it. Added `READERSHIP.md`, the vertical view: one statement both lanes render, the four things that survive the change of reader, and the residue in both directions that stays unreconciled. `llms.txt` stays the agent entry point, a published convention that a new document should not absorb; it now points at the agent lane directly under the evidence ceiling and lists `index.html`, which it had never indexed. `JOURNEY.md` is named as the human lane without being rewritten. Re-addressed `README.md`, `TOOLBOX.md`, `BRIEF.md`, and `index.html` to match, and added the three files to `REQUIRED_ARCHITECTURE`. `FILMING-FORMS.md` lands in the human lane: its four forms are relational positions a model occupies none of, so the camera is another row with an empty agent cell, and naming the form in use joins the hand-back list. This is a change in how the kit is read. `CURRENT-EVIDENCE.md` is unchanged and `RESEARCH-STATUS.md` gains no row, because a readership is not a claim field evidence could weaken.

- Retired the adapted ensō-fork mark and replaced it with a fork diagram built from the papañca causal sequence (Majjhima Nikāya 18): one stem, two branches, no circle, no return reading, no two-tone ink/brass system. Updated `design/assets/mark-{light,dark}.svg`, `design/assets/banner-{light,dark}.svg`, `design/VISUAL-LANGUAGE.md`'s mark section, `PROVENANCE.md`'s naming-and-symbolism section, and `index.html`'s mark alt text and caption to match. Isaac chose the direction (replace, not retire or reinterpret) and the specific mark from four line-work options.

- Added RAIN (Recognize, Allow, Investigate, Non-Identification) to `PRACTICE-FRAME.md` as the practice loop's concrete method for working with a noticed condition, cited in `PROVENANCE.md` under a new "Modern practice tools" section kept separate from the canonical Buddhist sources above it. The citation states what's settled (Tara Brach's 2013 published wording) and what isn't (Michele McDonald's credited but undated origination; her own recorded wording differs from the phrasing used here). Added a research-status row for RAIN's usefulness in information practice, tracked the same way as the kit's other Buddhist-derived hypotheses.
- Added the Madhupiṇḍika Sutta's proliferation chain (Majjhima Nikāya 18) and the Abhidhamma's javāna account of the cognitive process to `PRACTICE-FRAME.md`, as the mechanism underneath the three poisons and five hindrances rather than a replacement for them; cited both in `PROVENANCE.md` and marked the looping-rumination reading as PureLand's own extension, not a claim the Abhidhamma source makes. Added a research-status row and a stated falsifying condition for each of the two new hypotheses (the chain's leverage claim, and the javāna-looping reading of rumination). Added `ORIGIN.md` as a placeholder for the maintainer's own, not-yet-written history with Zen practice, linked from `PROVENANCE.md`. Left the adapted ensō mark and its existing disclaimers unchanged; the naming-and-symbolism boundary already discloses the Zen association, and any change to the mark itself is a maintainer decision, not made in this pass.
- Tightened `llms.txt` after review. The evidence ceiling now separates its link from its description with a colon, so a consumer that parses list items into a link and a description keeps "do not state or imply a stronger result" rather than reading an empty description. The summary carries the evidence link itself, and no longer freezes the field-test count that goes stale on the next record; it states the durable limit instead, that the instruments are unvalidated and no independent field trial has been accepted. Rewrote the governance and contributing lines to the active sentences their source documents use, and dropped the release numerals from the research-status line, which the ledger itself holds. Added a `scripts/check_repo.py` check that ties the release version `README.md` and `llms.txt` announce to `CITATION.cff`, so a release cannot bump one and leave an entry point announcing the old version. The check reads only the "This is version N.M." sentence, because the remaining numeral names the method contract, which does not move with a release.
- Added `llms.txt` as the model-facing entry point, with absolute Pages links, the current evidence ceiling, rights and repository boundaries, and the research-status ledgers kept in the primary path. Extended `scripts/check_repo.py` so the file is required and included in link, placeholder, claim-wording, and reachability checks.
- Added the open-model lane: `OPEN-MODEL-LANE.md` (running open-weight models on owned hardware, credential discipline for hosted gateways, and the paired-bench method for testing model quality claims yourself), `scripts/llm-lane.sh` (adaptable shell functions for the local tiers), and `templates/bench-card.yaml` (the bench declaration format). Generalized from the maintainer's private stack with hostnames, models, and credentials replaced by examples; the reference bench harness stays private, a named boundary in the document itself.

- Reframed PureLand as a version 0.1 research-program prototype with six public functions: thesis, method, toolbox, hypothesis, testing, and current evidence. PureLand remains the program name, and the existing forkable instruments remain the toolbox.
- Integrated the architecture, measurement, and rights lanes; removed four unpublished compatibility layers; made public issues Stage 1 scoping only; replaced the Boolean privacy check with an artifact-version public-safe review; and separated documented intentional deletion from missing provenance.
- Narrowed FT-001 and Alchemy claims, removed an unsupported information-theory contribution, replaced the three-layer visual motif, and added boundaries around the PureLand name, adapted ensō, generosity definition, and attention-sovereignty language.
- Wired `index.html` up as the repository's front page. It links out to the kit's documents instead of standing with none, and it consumes `design/tokens.css` and the marks in `design/assets/` instead of forking them.
- Dropped the Google Fonts request from `index.html` in favor of the documented fallback stacks, matching the no-servers posture the rest of the kit describes.
- Replaced `index.html`'s superseded three-layer ledger with the six stations from JOURNEY.md, which holds the canonical list. Corrected `design/VISUAL-LANGUAGE.md`, which still described the roman numerals as standing for three layers.
- Set the `index.html` hero question in sentence case. In letterspaced capitals it ran to eleven lines and shouted, against the first principle in `design/VISUAL-LANGUAGE.md`. The short section headings stay uppercase.
- Fixed the `index.html` spread layout below 860px. Right-side marginalia stayed pinned to grid column 3, which collapsed the passage column to about 115px on a phone.
- Fixed a dark-mode contrast defect in `design/tokens.css`: `--color-text-secondary` was Graphite on Ink at 2.95:1, failing WCAG AA. Added the `--measure` and `--color-rule` tokens `index.html` needed, and made `--rule-hairline-color` an alias of `--color-rule` so the two cannot diverge.
- Added `.nojekyll` so Pages serves the repository as static files with no build step. Enabling the Pages source is a repository setting and was left alone.
- Widened `scripts/check_repo.py`. It now reads HTML links, validates field-test records against `data/field-test.schema.json`, resolves anchor fragments, scans more file types for placeholder tokens, and warns when a content file is unreachable from README.md, JOURNEY.md, or BRIEF.md.
- Moved schema validation onto `jsonschema` (Draft 2020-12) and added `requirements.txt`, which CI now installs. The hand-rolled validator it replaces covered nine keywords; the research-arc schema uses `$ref`, `$defs`, thirteen `allOf`/`if`/`then` pairs, `not`, and `contains`, and those applicators are where a hand-rolled validator fails open on the privacy gate. `scripts/check_repo.py` reports the missing dependency as an error rather than skipping validation.
- Scoped JOURNEY.md's Alchemy claims to what FT-001 supports, and matched README's description of the app to it. The browser-only claim covers the PWA surface; two other surfaces send material out.
- Made PROVENANCE.md and AI-ASSISTANCE.md reachable from the README by linking them from research/README.md. Linked the provenance record from BRIEF.md and data/README.md from CONTRIBUTING.md, and pointed README at `index.html`.
- Reconciled README's description of FIELD-TESTING.md with JOURNEY.md: it is the instrument the ground and return stations use directly, not a discipline that only runs alongside them.
- Fixed CITATION.cff, which named CC BY-SA 4.0 as the sole license. It now lists both MIT and CC BY-SA 4.0, matching the license split recorded everywhere else. The version and release date are unchanged; the version 0.2 gate in RESEARCH-STATUS.md has not been cleared.
- FT-001's bounded adaptation executed on owner consent: the Alchemy README now documents the inbound route for corrections and field reports (alchemy PR #15). The record moves from proposed to executed; the 90-day window runs to the 2026-11-22 review. The record also notes it keeps the access names it was scored under.
- Rewrote the public documents in plainer language at roughly a 12th-grade reading level, keeping every guardrail, claim boundary, and file name intact.
- Replaced "the four accesses" and their formal names (legibility, permeability, forkability, provenance) with four plain questions: understandable (what can be known), reachable (within arm's reach), adaptable (accepting and investigating the reality of change), traceable (we can see what happened, sources attributed, connected to an open record).
- Recorded FT-001, a maintainer-side partial dry run of the journey on Alchemy. It is not independent. The report lives in `research/field-tests/`, its structured record in `data/field-tests/`, and its ledger entry in `FIELD-TRIALS.md`.
- Consolidated the entry path into one curated journey (`JOURNEY.md`). The instruments remain intact as stations on it, and the README now opens with the journey instead of a menu of instruments.
- Connected the Alchemy app into the journey as the companion for walking it on your own information ecosystem: the diagnostic as a station-1 prompt, the capture loop as daily observe practice, the app itself as a station-3 and station-4 subject.
- Added `journey` to the field-test instrument options in the schema, issue form, and template, so a report can cover the whole walk.
- Reorganized the public surface around goals, method, accountability, the present field-pilot offering, and evidence still to be tested.
- Added `OFFERING.md` for the bounded PureLand Field Pilot and `research/README.md` for the public open-research lane.
- Kept the workspace research reservoir, participant material, and other protected records outside the public release surface.
- Added the Ink & Bone visual language, design tokens, the ensō-fork mark, and a README banner. See [design/VISUAL-LANGUAGE.md](design/VISUAL-LANGUAGE.md).
- Reframed the kit around attention sovereignty, three poisons, five hindrances, skillful means, greed, and generosity.
- Added consent, AI-system, research-status, field-testing, rights, contribution, and governance guidance.
- Retired the unvalidated extraction coefficient and composite thresholds.
- Added canonical CC BY-SA 4.0 legal text and clarified license boundaries.
- Added validation automation and structured field-test templates.

## 0.1.0

- Initial protocol, scorecard, extraction check, provenance, and license explanation.
